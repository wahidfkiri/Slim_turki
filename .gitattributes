# Set default behavior to auto-convert
* text=auto

# Explicitly declare text files you want to always be normalized and converted
*.php text
*.js text
*.css text
*.html text
*.json text
*.md text
*.txt text
*.yml text
*.yaml text

# Denote files that should always have CRLF line endings on checkout
*.bat text eol=crlf

# Denote files that should always have LF line endings on checkout
*.sh text eol=lf

# Files that are binary should not be modified
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary